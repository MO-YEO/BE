spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:testdb
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-google-client-id
            client-secret: test-google-client-secret

app:
  oauth2:
    redirect-url: http://localhost:5173/oauth/callback
  jwt:
    secret: test-jwt-secret-key-for-context-loads-must-be-long-enough
    access-exp-min: 60

gemini:
  api-key: test-gemini-api-key
